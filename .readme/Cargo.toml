workspace.members = ["."]

[package]
name = "readme"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
password-hash = "0.4"
argon2 = { path = "../argon2" }
pbkdf2 = { path = "../pbkdf2", features = ["simple"] }
scrypt = { path = "../scrypt" }
