language: rust
rust:
  - stable
  - nightly
matrix:
  include:
    script: cd scrypt; cargo test --tests --verbose --no-default-features
  allow_failures:
    - rust: nightly
script: cargo test --verbose --all
